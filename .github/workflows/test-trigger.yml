name: Test Trigger

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "✅ GitHub Actions is working!"
          echo "Event: ${{ github.event_name }}"
          echo "Ref: ${{ github.ref }}"
          date