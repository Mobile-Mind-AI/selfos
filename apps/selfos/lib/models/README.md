# Models

This directory contains data models and DTOs for the SelfOS Flutter app.

## Structure
- `user.dart` - User model
- `goal.dart` - Goal model  
- `task.dart` - Task model
- `life_area.dart` - Life area model
- `api_response.dart` - API response wrapper models